## 📚 API Documentation

Once the server is running, you can access the interactive Swagger documentation at:
http://localhost:3000/v1/api-docs

## 1. backend

1. cd backend
2. npm install
3. connect you posgresql DB
4. change .env.example to .env
5. fill username and password DB in .env 
4. npm run db:migrate
5. npm run dev (for dev) or npm run prod (for production)

## 2. frontend

1. cd frontend
2. npm install
3. change .env.example to .env
4. npm run dev

## login

email: [EMAIL_ADDRESS]
password: [PASSWORD]
